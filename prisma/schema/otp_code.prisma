model OtpCode {
  id        String   @id @default(uuid())
  userEmail String
  code      String
  expiresAt DateTime
  consumed  Boolean  @default(false)
  createdAt DateTime @default(now())

  @@index([userEmail])
  @@map("otp_code")
}
